[package]
name = "token-leaderboard"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "22.0.1"

[dev_dependencies]
soroban-sdk = { version = "22.0.1", features = ["testutils"] }
